
#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main(){
    string a, b, c;
    cin >> a >> b >> c;
    cout << char(a.at(0) + 'A' - 'a') << char(b.at(0) + 'A' - 'a') << char(c.at(0) + 'A' - 'a');
    
}