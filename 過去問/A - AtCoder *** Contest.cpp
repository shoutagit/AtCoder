
#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)

int main(){
    string a, b, c;
    cin >> a >> b >> c;
    b = b.at(0);
    cout << "A" + b + "C"  << endl;
    
}